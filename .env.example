# Példa környezeti változók - másold át .env fájlba és töltsd ki az értékeket

# Adatbázis (opcionális - jelenleg memóriában tároljuk az adatokat)
# DATABASE_URL=postgresql://username:password@hostname:port/database

# Port beállítás (alapértelmezett: 5000)
# PORT=5000

# Node.js környezet
NODE_ENV=production